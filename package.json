{
  "name": "4pulse",
  "version": "1.9.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs all",
    "build:firefox": "node scripts/build.mjs firefox",
    "build:chrome": "node scripts/build.mjs chrome",
    "dev": "node scripts/dev-chromium.mjs popup",
    "dev:all": "node scripts/dev-chromium.mjs all",
    "dev:options": "node scripts/dev-chromium.mjs options",
    "dev:sidebar": "node scripts/dev-chromium.mjs sidebar",
    "setup:browser": "playwright install chromium",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.5.0",
    "globals": "^17.6.0",
    "playwright": "^1.61.0",
    "vitest": "^4.1.9"
  }
}
